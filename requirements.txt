
    fastapi==0.115.0
    uvicorn[standard]==0.30.6
    numpy==2.0.1
    soundfile==0.12.1
    librosa==0.10.1
    # Optional heavy deps are imported lazily (only used when BELT_SAFE_MODE=0)
    torch==2.3.1+cpu ; platform_system != "Darwin"
--find-links https://download.pytorch.org/whl/cpu
    torchaudio==2.3.1+cpu ; platform_system != "Darwin"
--find-links https://download.pytorch.org/whl/cpu
